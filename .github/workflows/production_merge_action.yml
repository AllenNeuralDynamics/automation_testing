name: production-merge-action
run-name: ${{ github.actor }} is testing out Github Actions
on:
    schedule:
    - cron: '*/2 * * * *'
jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
            - run: echo "The job was auto triggered by a schedule"

